# Possession Log: Azazel — Engineer

**Possession ID:** 11
**Daemon:** Azazel
**Role:** Engineer
**Status:** ACTIVE
**Started:** 2026-04-03T23:20:14

---

## Task

**ENG-H-0253** — Remove or retire old `mission_*` and `persona_*` OpenCode tools

**Epic:** EPC-H-0008 (Daemon/Possession Redesign)

---

## Summary

Completed the removal of all `mission_*` and `persona_*` tool files from
`.opencode/tools/` and updated parameter vocabulary in all remaining tools.

### Tools Deleted

- `mission_init.py` / `.ts`
- `mission_end.py` / `.ts`
- `mission_role_record.py` / `.ts`
- `mission_persona_record.py` / `.ts`
- `mission_dashboard.py` / `.ts`
- `mission_summary.py` / `.ts`
- `mission_rename_session.py` / `.ts`
- `mission_rename_dismissed.py` / `.ts`
- `persona_show.py` / `.ts`
- `persona_suggest.py` / `.ts`
- `persona_set_bio.py` / `.ts`

### Tools Updated (vocabulary migration)

- `worker_terminate.ts` / `.py` — `from_mission_id`/`to_mission_id` → `from_possession_id`/`to_possession_id`
- `worker_spawn.ts` / `.py` — `persona` → `daemon`
- `task_claim.ts` / `.py` — backward compat for `possession_id` OR `mission_id`
- `task_show.ts` — `mission_id` → `possession_id` in schema
- `push_status.ts` / `.py` — updated param descriptions and backward compat
- `watch_inbox.ts` / `.py` — renamed vars, backward compat, updated output keys

### Skills Updated

- `.opencode/skills/mission-end/SKILL.md` — deprecation notice added
- `.opencode/skills/mission-start/SKILL.md` — deprecation notice added

### Tests Fixed

- `tests/test_desk_worker_script.py` — `--persona` → `--daemon`, `worker.persona` → `worker.daemon`, `mission-end` → `possession-end`
- `tests/test_tools_phase2.py` — `@_SKIP_REMOVED` applied to `TestPersonaSuggest`, `TestPersonaSetBio`, `TestMissionDashboard` (and others in prior sessions)
- `tests/test_mission_init_e2e.py` — `pytestmark` skip applied to entire module

### Commits

- `4a83f05` — prior session work (bulk tool migration)
- `61556a5` — skill cleanup
- `61dac0e` — test fixes and final cleanup (this session)

---

## Test Results

46 passed, 83 skipped, 0 failed
